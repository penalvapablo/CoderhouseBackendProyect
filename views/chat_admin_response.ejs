<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x"
      crossorigin="anonymous"
    />
    <title>Chat</title>
  </head>
  <body class="container">
    <div class="container-fluid">
      <div class="chat pt-4 mb-2">
        <h2 class="mb-4 mt-4">Chat</h2>
        <div class="chat-form">
          <div class="chat-messages mb-3">
            <div class="message-item">
              <span class="email"><%= message.email %></span>
              <span> [<span class="date"><%= message.date %></span>]: </span>
              <span class="message"><%= message.text %></span>

              <form
                class="form"
                action="/chat/admin/<%= message.id %>"
                method="post"
                role="form"
                autocomplete="off"
              >
                <div class="form-group">
                  <input
                    id="response"
                    name="response"
                    class="form-control"
                    type="text"
                    required
                  />
                </div>
                <div class="form-group">
                  <button class="btn btn-success my-3 submit" type="submit">
                    Response
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
